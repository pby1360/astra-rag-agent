# Astra-RAG-Agent 구현 계획

> **프로젝트**: 항공우주·방산 기술 규격(MIL-SPEC) 교차 검증을 위한 LangGraph 기반 RAG 시스템
> **기간**: 3주
> **목표**: 평가 만점(35점) + 가산점(+2~3점) 확보

---

## 전체 구조 (8단계)

```
[Week 1] Phase 1 ─ 프로젝트 셋업
         Phase 2 ─ 데이터 준비 (가장 시간 많이 듬)
         Phase 3 ─ 최소 RAG 동작 (벡터 검색 + 답변)
[Week 2] Phase 4 ─ Streamlit UI + 출처 출력
         Phase 5 ─ 하이브리드 검색 (BM25 결합)
         Phase 6 ─ LangGraph 도입
[Week 3] Phase 7 ─ 시나리오 안정화 + 시연 준비
         Phase 8 ─ 문서 작성 + 발표 리허설
```

## 최종 아키텍처

```
[Streamlit Web UI]
        ↓
[FastAPI 단일 서버]   ← Spring Boot 대체 (선택사항, Streamlit만으로도 충분)
        ↓
[LangGraph 워크플로우]
   ├─ Node 1: Query Analyzer  (질의 정규화 + 한영 키워드 추출)
   ├─ Node 2: Hybrid Retriever (Dense + BM25)
   ├─ Node 3: Verifier         (근거 충분성 판단 → 재검색 분기)
   └─ Node 4: Reporter         (출처 포함 한국어 응답 생성)
        ↓
[Chroma 단일 벡터 DB] + [BM25 인메모리 인덱스]
        ↓
[/data: MD 30+ / JSON 부품 / YAML 용어집]
```

---

## Phase 1: 프로젝트 셋업 (Day 1, 반나절)

**목표**: 빈 프로젝트에서 LLM API 호출 + 임베딩이 동작하는 것까지.

### 작업

1. 프로젝트 디렉토리 구조 생성
   ```
   astra-rag/
     /data/standards/      (MD 파일들)
     /data/components/     (JSON)
     /data/glossary/       (YAML)
     /src/
       /ingestion/         (데이터 적재)
       /retrieval/         (검색 로직)
       /graph/             (LangGraph 노드들)
       /ui/                (Streamlit)
     /chroma_db/           (벡터 DB 저장소, gitignore)
     /tests/
     .env
     requirements.txt
     README.md
   ```

2. Python 가상환경 + 패키지 설치
   ```
   chromadb, langchain, langchain-community, langgraph,
   sentence-transformers, rank-bm25, streamlit,
   pypdf or pdfplumber, python-dotenv, pyyaml,
   anthropic or openai
   ```

3. `.env`에 API 키 세팅, `.gitignore` 작성

4. **smoke test 스크립트 1개**: `python tests/smoke_test.py` 실행 시 LLM API 호출 1회 + 임베딩 1회가 성공해야 함

### 검증 기준
- smoke test 통과. 이거 안 되면 다음 단계 모두 막힘.

---

## Phase 2: 데이터 준비 (Day 2-4, 가장 중요)

**목표**: 30+ 개의 참조 자료 파일을 갖춘 `/data` 디렉토리 완성.

이 단계가 사실상 프로젝트의 절반. 데이터 품질이 RAG 성능을 좌우함.

### 2-1. PDF 다운로드 (1시간)
- MIL-STD-810H, MIL-STD-883, MIL-STD-461G, SMC-S-016 PDF를 `/raw_pdfs/`에 저장
- 부품 데이터시트 2종 (TPS7H1101-SP, SAMV71Q21RT) 다운로드

### 2-2. PDF → 마크다운 변환 (반나절~하루)

옵션 두 가지 중 선택:
- **옵션 A**: LlamaParse (품질 좋음, 무료 크레딧 한도 있음). API 키 받아서 사용.
- **옵션 B**: `pdfplumber` + 후처리 스크립트 (무료, 품질 살짝 떨어짐). 표가 많지 않은 섹션은 충분.

**추천**: MIL-STD-810H처럼 표가 많은 핵심 문서만 LlamaParse, 나머지는 pdfplumber. 무료 크레딧 아끼기.

### 2-3. 섹션별 분할 (하루)

자동 분할 스크립트 작성:
```python
# src/ingestion/split_standards.py
# MIL-STD-810H의 "METHOD 5XX" 헤더 정규식으로 split
# 각 메서드를 별도 .md 파일로 저장
```

수동 보정 필요. 각 파일 상단에 frontmatter 추가:
```markdown
---
standard: MIL-STD-810H
method: "514.8"
category: vibration
language: en
---

# Method 514.8 — Vibration
...
```

### 2-4. 부품 JSON 작성 (반나절)

데이터시트에서 핵심 스펙만 뽑아 JSON으로 구조화:
```json
{
  "part_number": "TPS7H1101-SP",
  "manufacturer": "Texas Instruments",
  "category": "power_regulator",
  "specs": {
    "tid_tolerance_krad": 100,
    "operating_temp_min_c": -55,
    "operating_temp_max_c": 125,
    "vibration_g_rms": 14.1
  },
  "compliance": ["MIL-STD-883 Method 1019"],
  "source_url": "https://www.ti.com/product/TPS7H1101-SP"
}
```

실제 2개 + 시나리오용 가상 부품 3~5개 = 총 5~8개 파일.

### 2-5. YAML 용어집 (1~2시간)

한영 매핑 30+ 항목. 시나리오 3개에서 쓸 용어 + 일반 용어로 채우기.

### 검증 기준
- `ls /data/standards/*.md | wc -l` ≥ 25
- `ls /data/components/*.json | wc -l` ≥ 5
- YAML 항목 30+ 개
- **각 MD 파일에 frontmatter 메타데이터 있음**
- 시나리오 3개의 핵심 키워드(`Salt Fog`, `TID`, `Vibration Category 24`)가 모두 데이터에 존재하는지 grep으로 확인

---

## Phase 3: 최소 RAG 동작 (Day 5-7)

**목표**: 커맨드라인에서 질문 → 답변 + 출처 출력까지. UI는 아직 없음.

### 3-1. 문서 로딩 + 청킹 (Day 5 오전)

```python
# src/ingestion/loader.py
# 1. /data/standards/*.md 로딩, frontmatter 메타로 분리
# 2. /data/components/*.json 로딩, 각 spec을 텍스트로 직렬화
# 3. /data/glossary/*.yaml 로딩, 각 항목을 mini-doc으로 변환
# 4. LangChain MarkdownTextSplitter 또는 RecursiveCharacterTextSplitter
#    chunk_size=500, overlap=50 정도부터 시작
```

각 청크가 가져야 할 메타데이터:
```python
{
  "source": "mil-std-810h_method_514_vibration.md",
  "standard": "MIL-STD-810H",
  "method": "514.8",
  "category": "vibration",
  "chunk_index": 3,
}
```

### 3-2. 임베딩 + Chroma 적재 (Day 5 오후)

```python
# src/ingestion/embed_and_store.py
# 1. SentenceTransformer로 임베딩
#    - 모델: "BAAI/bge-m3" (멀티링구얼, 한영 모두)
#    - 메모리 부족하면 "BAAI/bge-small-en-v1.5" + 한국어는 별도 처리
# 2. Chroma persistent client로 저장
#    chroma_client = chromadb.PersistentClient(path="./chroma_db")
# 3. collection.add(documents, embeddings, metadatas, ids)
```

**임베딩 모델 결정 가이드**:
- GPU 8GB+ 있음 → BGE-M3 (원 설계 그대로)
- GPU 없음/4GB → `intfloat/multilingual-e5-base` (가벼움, 한영 모두 OK)
- API 가능 → OpenAI `text-embedding-3-small` (가장 안정적, 비용 저렴)

### 3-3. 검색 함수 (Day 6 오전)

```python
# src/retrieval/retriever.py
def retrieve(query: str, k: int = 5, filter: dict = None) -> list[dict]:
    # 1. query 임베딩
    # 2. chroma collection.query()
    # 3. return [{"text": ..., "metadata": ..., "score": ...}, ...]
```

테스트:
```python
results = retrieve("salt fog test coating thickness")
for r in results:
    print(r["metadata"]["source"], r["score"])
    print(r["text"][:200])
```

### 3-4. 답변 생성 함수 (Day 6 오후)

```python
# src/retrieval/generator.py
PROMPT_TEMPLATE = """
당신은 항공우주·방산 규격 검증 어시스턴트입니다.
아래 [참조 문서]만을 근거로 사용자 질문에 답변하세요.
참조 문서에 없는 내용은 "정보 없음"으로 답하고 추측하지 마세요.

[참조 문서]
{context}

[질문]
{question}

[답변 형식]
- 답변 본문
- 근거: 각 주장의 출처 파일명과 핵심 문장
"""

def generate_answer(question: str, retrieved_docs: list) -> dict:
    context = "\n\n".join([
        f"[{i+1}] {d['metadata']['source']}\n{d['text']}"
        for i, d in enumerate(retrieved_docs)
    ])
    # LLM 호출
    return {
        "answer": llm_response,
        "sources": [d['metadata'] for d in retrieved_docs]
    }
```

### 3-5. End-to-end 스크립트 (Day 7)

```python
# tests/e2e_cli.py
while True:
    q = input("질문> ")
    if q == "exit": break
    docs = retrieve(q)
    result = generate_answer(q, docs)
    print(result["answer"])
    print("\n참조:", [s["source"] for s in result["sources"]])
```

### 검증 기준
- 시나리오 3개 질문이 모두 합리적인 답변을 내놓음
- 출처 파일명이 정확히 출력됨
- 할루시네이션 없음 (없는 정보는 "정보 없음"이라고 답함)

> 🎯 **이 시점에 이미 평가표 1-1(10점), 1-2(5점), 1-3(5점)의 대부분 확보. 그룹2(5점)도 확보. 즉 25점 보장.**

---

## Phase 4: Streamlit UI + 출처 출력 정교화 (Day 8-9)

**목표**: 웹 UI에서 채팅 형태로 동작 + 출처가 깔끔하게 보임.

### 4-1. 기본 Streamlit 채팅 (Day 8 오전)

```python
# src/ui/app.py
import streamlit as st

st.title("Astra RAG — 항공우주·방산 규격 검증")

if "messages" not in st.session_state:
    st.session_state.messages = []

for msg in st.session_state.messages:
    with st.chat_message(msg["role"]):
        st.markdown(msg["content"])

if prompt := st.chat_input("질문을 입력하세요"):
    st.session_state.messages.append({"role": "user", "content": prompt})
    with st.chat_message("user"):
        st.markdown(prompt)

    with st.chat_message("assistant"):
        with st.spinner("검색 중..."):
            docs = retrieve(prompt)
            result = generate_answer(prompt, docs)
        st.markdown(result["answer"])
        # 출처 expander
        with st.expander(f"참조 문서 {len(docs)}개"):
            for i, d in enumerate(docs):
                st.markdown(f"**[{i+1}] {d['metadata']['source']}**")
                st.caption(f"표준: {d['metadata'].get('standard', 'N/A')} | 점수: {d['score']:.3f}")
                st.text(d['text'][:300] + "...")

    st.session_state.messages.append({"role": "assistant", "content": result["answer"]})
```

### 4-2. 출처 표시 다듬기 (Day 8 오후)

평가표 1-2의 핵심 = 출처가 명확해야 함. 이 정도까지 다듬으면 만점:
- 파일명 + 표준명 + 메서드 번호 + 일부 텍스트 + 유사도 점수
- 각 출처가 답변의 어느 부분을 뒷받침하는지 표시 (가능하면)

### 4-3. 시연용 부가 기능 (Day 9)

발표에 점수 받기 좋은 요소들:
- 사이드바에 "예시 질문" 버튼 (시나리오 3개)
- 검색 결과 개수, 응답 시간 표시
- "검색 로그" 토글 (다음 Phase에서 LangGraph 노드 실행 과정 보여줄 자리)

### 검증 기준
- `streamlit run src/ui/app.py`로 동작
- 시나리오 3개를 클릭으로 실행 가능

---

## Phase 5: 하이브리드 검색 (Day 10-11)

**목표**: Dense (벡터) + Sparse (BM25) 결합으로 검색 품질 향상.

> LangGraph 도입 전에 이거 먼저 하는 이유: **검색 로직이 안정돼야 LangGraph로 감쌌을 때 문제가 적음**. 검색 자체에서 문제가 나면 LangGraph 디버깅과 섞여서 원인 파악이 어려워짐.

### 5-1. BM25 인덱스 구축 (Day 10 오전)

```python
# src/retrieval/bm25_retriever.py
from rank_bm25 import BM25Okapi

class BM25Retriever:
    def __init__(self, docs: list[dict]):
        # docs: 청크 리스트, Phase 3과 동일한 데이터
        tokenized = [self._tokenize(d["text"]) for d in docs]
        self.bm25 = BM25Okapi(tokenized)
        self.docs = docs

    def _tokenize(self, text):
        # 영문은 lower + split, 한국어는 그대로 (또는 형태소 분석기 사용)
        return text.lower().split()

    def retrieve(self, query, k=5):
        scores = self.bm25.get_scores(self._tokenize(query))
        top_k = sorted(enumerate(scores), key=lambda x: -x[1])[:k]
        return [{"text": self.docs[i]["text"],
                 "metadata": self.docs[i]["metadata"],
                 "score": s} for i, s in top_k]
```

### 5-2. 하이브리드 결합 (Day 10 오후)

```python
# src/retrieval/hybrid.py
def hybrid_retrieve(query: str, k: int = 5, alpha: float = 0.6):
    """
    alpha: dense vs sparse 가중치. 0.6이면 dense 60%, sparse 40%
    """
    dense_results = vector_retrieve(query, k=10)
    sparse_results = bm25_retrieve(query, k=10)

    # Reciprocal Rank Fusion (RRF) 또는 단순 score normalization
    combined = rrf_combine(dense_results, sparse_results)
    return combined[:k]
```

**RRF 추천**: 점수 스케일이 다른 두 검색을 합칠 때 가장 안정적. 구현 5줄이면 됨.

### 5-3. 한영 쿼리 확장 (Day 11)

YAML 용어집을 활용해 검색 전에 쿼리 확장:
```python
def expand_query_ko_en(query: str, glossary: dict) -> str:
    expanded = query
    for ko, en in glossary.items():
        if ko in query:
            expanded += f" {en}"
    return expanded

# "염수 분무 테스트 코팅" → "염수 분무 테스트 코팅 Salt Fog Test"
```

이게 단순해 보여도 한영 혼재 문서에서 효과가 큼. 시나리오 3의 핵심.

### 검증 기준
- 시나리오 3 ("염수 분무 테스트")에서 영문 MD 파일이 상위에 검색되는지 확인
- Phase 3 대비 검색 품질이 눈에 띄게 개선되어야 함

---

## Phase 6: LangGraph 도입 (Day 12-14)

**목표**: 검색-검증-재검색 흐름을 LangGraph로 구조화.

> 핵심: **기존 함수들을 그대로 노드로 감싼다**. 새로 짜지 말 것.

### 6-1. State 정의 (Day 12 오전)

```python
# src/graph/state.py
from typing import TypedDict, Optional

class RAGState(TypedDict):
    question: str
    expanded_query: Optional[str]
    retrieved_docs: list
    is_sufficient: bool
    retry_count: int
    answer: Optional[str]
    sources: list
    trace: list  # 발표 시 노드 실행 로그
```

### 6-2. 노드 함수 작성 (Day 12 오후 ~ Day 13)

```python
# src/graph/nodes.py
def analyze_node(state: RAGState) -> dict:
    expanded = expand_query_ko_en(state["question"], glossary)
    return {
        "expanded_query": expanded,
        "trace": state["trace"] + [f"[Analyze] 쿼리 확장: {expanded}"]
    }

def retrieve_node(state: RAGState) -> dict:
    query = state["expanded_query"] or state["question"]
    # 재시도 시 검색 파라미터 조정
    k = 5 if state["retry_count"] == 0 else 10
    docs = hybrid_retrieve(query, k=k)
    return {
        "retrieved_docs": state["retrieved_docs"] + docs,  # 누적
        "trace": state["trace"] + [f"[Retrieve] {len(docs)}개 검색 (시도 {state['retry_count']+1})"]
    }

def verify_node(state: RAGState) -> dict:
    # LLM에게 "이 문서들로 질문에 답할 수 있는지" 판단 요청
    prompt = VERIFY_PROMPT.format(
        question=state["question"],
        docs=format_docs(state["retrieved_docs"])
    )
    judgment = llm.invoke(prompt)
    sufficient = parse_judgment(judgment)  # True/False
    return {
        "is_sufficient": sufficient,
        "trace": state["trace"] + [f"[Verify] 정보 충족: {sufficient}"]
    }

def generate_node(state: RAGState) -> dict:
    result = generate_answer(state["question"], state["retrieved_docs"])
    return {
        "answer": result["answer"],
        "sources": result["sources"],
        "trace": state["trace"] + ["[Generate] 답변 생성 완료"]
    }
```

### 6-3. 그래프 구성 + 조건 분기 (Day 13 오후)

```python
# src/graph/builder.py
from langgraph.graph import StateGraph, START, END

def should_retry(state: RAGState) -> str:
    if state["is_sufficient"]:
        return "generate"
    if state["retry_count"] >= 1:  # 최대 1회 재시도
        return "generate"
    return "retrieve_again"

def build_graph():
    g = StateGraph(RAGState)
    g.add_node("analyze", analyze_node)
    g.add_node("retrieve", retrieve_node)
    g.add_node("verify", verify_node)
    g.add_node("generate", generate_node)

    g.add_edge(START, "analyze")
    g.add_edge("analyze", "retrieve")
    g.add_edge("retrieve", "verify")
    g.add_conditional_edges(
        "verify",
        should_retry,
        {
            "generate": "generate",
            "retrieve_again": "retrieve"  # retry_count 증가는 retrieve 내부에서
        }
    )
    g.add_edge("generate", END)

    return g.compile()
```

> **주의**: retry_count는 retrieve 노드 진입 시 증가시켜야 함. verify에서 분기 시 retry_count 검사.

### 6-4. Streamlit 연결 + trace 시각화 (Day 14)

```python
# src/ui/app.py 수정
graph = build_graph()

if prompt := st.chat_input("..."):
    init_state = RAGState(
        question=prompt, retrieved_docs=[],
        retry_count=0, trace=[], ...
    )

    # stream으로 노드 실행 과정 실시간 표시
    trace_container = st.empty()
    for step in graph.stream(init_state, config={"recursion_limit": 10}):
        node_name = list(step.keys())[0]
        trace_container.info(f"⚙️ {node_name} 실행 중...")

    final_state = step[node_name]
    # 답변, 출처, trace 모두 표시
```

### 검증 기준
- 시나리오 3개 모두 graph.invoke 또는 stream으로 정상 종료
- Self-Correction이 트리거되는 케이스 1개 이상 확인 (의도적으로 데이터에 없는 정보를 물어봐서 재검색 발생시키기)
- 무한루프 없음 (`recursion_limit` 도달 안 함)

> 🎯 **이 시점에 LangGraph 시연 가능. 가산점 +1~2 확보.**

---

## Phase 7: 시나리오 안정화 + 시연 준비 (Day 15-17)

**목표**: 시연에서 절대 깨지지 않는 5~7개 질문 확정.

### 7-1. 시연 질문 셋 확정 (Day 15)

각 시나리오마다 2~3개 질문 변형 준비:
```yaml
# tests/demo_questions.yaml
scenario_1:
  primary: "MIL-STD-810H Method 514 vibration test category 24의 PSD 값은?"
  variants:
    - "진동 규격 카테고리 24의 기준치를 알려줘"
    - "What is the PSD value for MIL-STD-810H vibration category 24?"
scenario_2: ...
scenario_3: ...
```

각 질문에 대해 **기대 답변 + 기대 출처 파일** 명시. 회귀 테스트용.

### 7-2. 회귀 테스트 자동화 (Day 16 오전)

```python
# tests/regression.py
def test_demo_questions():
    for q in load_demo_questions():
        result = graph.invoke({"question": q.text, ...})
        assert q.expected_source in [s["source"] for s in result["sources"]]
        assert q.expected_keyword in result["answer"]
```

매일 한 번 돌려서 회귀 발생하면 즉시 인지.

### 7-3. 실패 케이스 수정 (Day 16 오후)

지금까지 동작 안 한 질문들이 있을 것. 해결 옵션:
- 청킹 크기 조정
- 검색 k 늘리기
- 프롬프트 수정
- 데이터 추가/보강
- alpha (dense vs sparse 가중치) 조정

### 7-4. 안전망 추가 (Day 17)

시연 중 LLM API 실패 등에 대비:
```python
# 1. LLM 호출 실패 시 → 검색된 청크 원문만이라도 출력
# 2. 임베딩 모델 로딩 실패 시 → 사전 캐시된 임베딩 사용
# 3. 타임아웃 30초 설정
```

### 검증 기준
- 시연 질문 모두 3회 연속 성공
- 한 번이라도 실패 시 원인 파악 후 수정

---

## Phase 8: 문서 + 발표 (Day 18-21)

**목표**: 12+ 페이지 문서 (업데이트 표기 포함) + 10분 발표 준비.

### 8-1. 문서 업데이트 (Day 18-19)

기존 `과제_초안.md`를 베이스로:
- 각 섹션에 `> 🔄 [구현 결과 반영]` 박스로 변경 사항 추가
- **신규 섹션 추가**:
  - 8장. 구현 결과 (스크린샷, 시연 화면, trace 로그)
  - 9장. 설계 대비 변경 사항 (Knowledge Graph → Vector+BM25 하이브리드로 축소한 이유, MSA → 단일 서버로 축소한 이유 등 솔직하게)

### 8-2. 발표 자료 (Day 20)

PPT 의무는 아니지만 만들면 좋음:
- 슬라이드 1: 문제 정의 (1분)
- 슬라이드 2: 아키텍처 다이어그램 (1분)
- 슬라이드 3: LangGraph 노드 흐름도 (1분)
- 라이브 시연 (5분, Streamlit 화면 공유)
- 슬라이드 4: Self-Correction 발동 케이스 (1분)
- 슬라이드 5: 한계 + 향후 발전 (1분)

### 8-3. 리허설 (Day 21)

타이머 켜고 3회 이상 리허설. 시연 중 막혔을 때 대처 시나리오까지 연습.

---

## 평가 항목 대응 매핑

| 평가 항목 | 배점 | 확보 시점 | 대응 방안 |
|---|---|---|---|
| 1-1. 에러 없이 반복 동작 | 10 | Phase 3-4 | CLI/UI 동작 + try-except 안전망 |
| 1-2. 참조 문서명·내용 출력 | 5 | Phase 4 | Streamlit expander로 출처 노출 |
| 1-3. 의도한 결과 출력 | 5 | Phase 7 | 시연 질문 회귀 테스트 |
| 그룹2. 참조 자료 양질 | 5 | Phase 2 | MD 25+/JSON 5+/YAML 30+ 항목 |
| 그룹3. 문서 12+ 페이지 | 5 | Phase 8 | 기존 문서 + 구현결과 섹션 추가 |
| 그룹4. 10분 발표 | 5 | Phase 8 | 라이브 시연 중심 + 리허설 3회 |
| **가산점**: 활용도 | +1 | Phase 5-6 | 하이브리드 검색 + 한영 확장 |
| **가산점**: 완성도 | +1~3 | Phase 6 | LangGraph Self-Correction 시연 |

**예상 총점**: 35점 + 가산점 2~3점

---

## 데이터 파일 30+개 만드는 구체적 방법

### MD 파일 분할 전략 (목표 25개)

```
/data/standards/
  mil-std-810h_method_500_low_pressure.md
  mil-std-810h_method_501_high_temperature.md
  mil-std-810h_method_502_low_temperature.md
  mil-std-810h_method_503_temperature_shock.md
  mil-std-810h_method_507_humidity.md
  mil-std-810h_method_509_salt_fog.md         ← 시나리오 3에서 사용
  mil-std-810h_method_514_vibration.md        ← 시나리오 1에서 사용
  mil-std-810h_method_516_shock.md
  ... (810H에서만 15개 이상 가능)
  mil-std-883_method_1019_radiation_tid.md    ← 시나리오 2에서 사용
  mil-std-883_method_2002_mechanical_shock.md
  ... (883에서 5개)
  mil-std-461g_ce101_conducted_emission.md
  mil-std-461g_rs103_radiated_susceptibility.md
  ... (461G에서 5개)
  smc-s-016_section_6_environment.md
  ...
```

### JSON 부품 데이터 (5~8개)

```
/data/components/
  tps7h1101_sp.json
  samv71q21rt.json
  antenna_coating_a.json   (시나리오 3용 가상 부품)
  vibration_damper_b.json  (시나리오 1용 가상 부품)
  ...
```

### YAML 용어집 (1개 파일, 내부 30+ 항목)

```yaml
# /data/glossary/ko_en_terms.yaml
- ko: 염수 분무
  en: Salt Fog
  standard: MIL-STD-810H Method 509
- ko: 진동 시험
  en: Vibration Test
  standard: MIL-STD-810H Method 514
- ko: 총이온화선량
  en: Total Ionizing Dose (TID)
  standard: MIL-STD-883 Method 1019
# ... 30+ 항목
```

---

## 임베딩 모델 결정 체크리스트

| 환경 | 추천 모델 | 비고 |
|---|---|---|
| GPU 8GB+ | `BAAI/bge-m3` | 원 설계 그대로, 한영 멀티링구얼 |
| GPU 없음/4GB | `intfloat/multilingual-e5-base` | 가벼움, 한영 모두 OK |
| API 가능 | OpenAI `text-embedding-3-small` | 가장 안정적, 비용 저렴 |

---

## LangGraph 구현 주의사항

1. **State는 가능한 단순하게**. dict 대신 `TypedDict` 사용. nested 구조 피하기.
2. **노드 함수는 반드시 State 일부를 반환**. 전체 state를 반환하면 덮어쓰기 이슈 생김. `return {"retrieved_docs": docs}` 이런 식.
3. **조건 분기 함수는 반드시 문자열 반환**. `def should_retry(state) -> str: return "retrieve" if ... else "generate"`. bool 반환하면 디버깅 지옥.
4. **`graph.invoke()` 결과는 최종 state**. 중간 단계 보려면 `graph.stream()` 사용. **시연 시 stream으로 보여주면 멋있음** — 각 노드 실행 과정이 실시간으로 찍힘.
5. **반드시 `recursion_limit` 설정**. `graph.invoke(input, config={"recursion_limit": 10})`. 무한루프 안전장치.

---

## 진행 체크리스트

### Week 1
- [ ] Phase 1: 프로젝트 셋업 완료
- [ ] Phase 2: 데이터 파일 30+개 준비 완료
- [ ] Phase 3: CLI에서 RAG 동작 확인

### Week 2
- [ ] Phase 4: Streamlit UI + 출처 출력 완료
- [ ] Phase 5: 하이브리드 검색 적용
- [ ] Phase 6: LangGraph 4노드 구조 동작

### Week 3
- [ ] Phase 7: 시연 질문 5~7개 안정화
- [ ] Phase 8: 문서 12+ 페이지 작성
- [ ] Phase 8: 발표 리허설 3회 완료

---

## 향후 발전 방향 (발표/문서용, 구현 X)

원 설계의 다음 요소들은 "향후 발전 방향"으로 문서에 포함하되 구현하지 않음:
- Neo4j 기반 Knowledge Graph (GraphRAG)
- 4-Agent Multi-Agent Self-Correction (현재는 3 노드 + 조건 분기로 축소)
- Spring Boot + FastAPI MSA 분리
- Human-in-the-Loop 지속 학습 파이프라인
- MLOps 무중단 배포

이 요소들은 **"왜 축소했는가"** 를 솔직하게 설명하는 것이 평가자에게 더 좋은 인상을 줌 (분석적이고 현실적인 엔지니어링 판단).
